Sources for the plugin `plugin.appodeal.beta.GoogleAdMob`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.GoogleAdMob" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
